apiVersion: v1
kind: Service
metadata:
  name: stenox-frontend-service
  namespace: dev
spec:
  type: ClusterIP
  selector:
    app: stenox-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80